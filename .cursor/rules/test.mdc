---
title: Testing Rules
description: Monorepo의 테스트 파일(.test.tsx/.spec.tsx)에만 자동으로 붙는 규칙
globs:
  - packages/**/*.test.ts
  - packages/**/*.test.tsx
  - packages/**/*.spec.ts
  - packages/**/*.spec.tsx
  - apps/**/*.test.ts
  - apps/**/*.test.tsx
  - apps/**/*.spec.ts
  - apps/**/*.spec.tsx
alwaysApply: false
---

# 🧪 Testing Rules for Monorepo

## 🎯 목적 (Purpose)

모노레포 내 `packages/`와 `apps/`의 모든 프로젝트에서 일관된 테스트 품질과 범위를 유지하기 위한 가이드라인이다.

- `packages/`: React 기반 공통 UI/유틸/훅 — Unit 테스트 중심
- `apps/`: React / Next.js 기반 앱 — Integration 및 E2E 테스트 포함
- 테스트 자동화 도구: **Vitest**, **Jest**, **Playwright (MCP optional)**

---

## ⚙️ 테스트 범위 (Scope)

| 구분        | 대상                           | 도구                               |
| ----------- | ------------------------------ | ---------------------------------- |
| Unit        | UI, Hooks, Utils               | `vitest`, `@testing-library/react` |
| Integration | 복합 UI, 페이지 내 데이터 흐름 | `vitest` 또는 `jest`               |
| E2E         | 전체 사용자 플로우             | `playwright`                       |
| Auto Gen    | 시나리오 초안 생성             | `playwright mcp`                   |

---

## 🧩 테스트 레벨 정의

### **Level 1: Unit Test (필수)**

- **대상:** 컴포넌트, 훅, 유틸 함수
- **도구:** `vitest`
- **원칙:**
  - 각 테스트 파일은 하나의 모듈만 검증
  - DOM 렌더링 시 `@testing-library/react` 사용
  - 스냅샷 테스트 최소화 → 행동 중심 assertion 사용

---

### **Level 2: Integration Test (권장)**

- **대상:**  
  폼, 모달 체인, 라우팅 간 상호작용 등 복합 UI
- **도구:**  
  `vitest` or `jest` + `@testing-library/react`
- **원칙:**
  - 사용자 액션 흐름을 기반으로 시나리오 작성
  - Mock은 외부 API 호출까지만 허용
  - 컴포넌트 간 결합은 실제로 유지

---

### **Level 3: E2E / Scenario Test (우선순위 기반)**

- **대상:**  
  로그인, 결제, 상품 생성, 대시보드 등 핵심 비즈니스 플로우
- **도구:**  
  `playwright`
- **원칙:**
  - 시나리오는 비즈니스 중요도(BIZ-01, BIZ-02 …)로 관리
  - 테스트 데이터는 fixture/seed로 고정
  - MCP 생성 시 반드시 리뷰 후 머지

---

## 🧮 테스트 우선순위 매트릭스

| 구분                 | 설명               | 테스트 수준                  |
| -------------------- | ------------------ | ---------------------------- |
| (B1, T1/T2)          | 핵심/복잡 로직     | **E2E + Integration + Unit** |
| (B1, T3) or (B2, T1) | 중간 중요/복합     | **Integration + Unit**       |
| (B2, T2/T3)          | 중간 이하          | **Unit 중심**                |
| (B3, \*)             | 저빈도/관리자 기능 | 필요 시 Unit                 |

---
